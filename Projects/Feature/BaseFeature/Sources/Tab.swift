public enum Tab {}

extension Tab {
    public enum PetOwner {
        case home
        case search
        case consulting
        case myPage
    }
    
    public enum Veterinarian {
        case home
        case customerReview
        case consultationList
        case myPage
    }
}
